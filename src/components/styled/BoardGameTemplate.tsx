import styled from "styled-components"

const BoardGame = styled.section`
    width: 400px;
    height: auto;
    display: grid;
    background: #fafafb;
    border-radius: 10px;
    padding: 20px 0;
    justify-content: center;
    align-content: center;
    grid-template-columns: repeat(3, auto);
`

export default BoardGame